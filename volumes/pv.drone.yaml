apiVersion: v1
kind: PersistentVolume
metadata:
  name: drone
spec:
  storageClassName: nfs-client
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
  nfs:
    path: "/mnt/pool/volumes/production/drone"
    server: data.pekware.xyz
    readOnly: false
