name: CI

on:
  push:
  workflow_dispatch:

jobs:
  build:
    runs-on:
      group: benchmark

    steps:
      - uses: actions/checkout@v4

      - name: Run a one-line script
        run: echo Hello, world!
